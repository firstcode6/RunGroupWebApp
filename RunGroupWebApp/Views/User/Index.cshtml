﻿@using RunGroupWebApp.ViewModels
@model List<UserVM>

<section class="flush-with-above">
    <div class="container">
        <div class="row">
            <div class="col">
                <table class="table table-hover align-items-center table-borderless">
                    <tbody>
                        @foreach (var user in Model)
                        {
                            <tr class="bg-white">
                 @*               <th scope="row">
                                    <div class="media align-items-center" style="height: 100px;">
                                        <img alt="Image profile avatar" src="@user.ProfileImageUrl" class="avatar" width="100" height="70">
                                        <div class="media-body">
                                            <span class="h6 mb-0">@user.UserName</span>
                                        </div>
                                    </div>
                                </th>*@
                                <td> <img alt="Image profile avatar" src="@user.ProfileImageUrl" class="avatar" width="100" height="100"></td>
                                <td> <span class="h6 mb-0">@user.UserName</span></td>
                                <td>
                                    <a class="btn btn-outline-primary" asp-controller="User" asp-action="Detail" asp-route-id="@user.Id">
                                        View Profile
                                    </a>
                                </td>

                                @{
                                    var mileage = user.Mileage == null ? "?" : user.Mileage.ToString();
                                    var pace = user.Pace == null ? "?" : user.Pace.ToString();
                                }
                                <td>@mileage mile at @pace mph</td>

                                @*<td><i class="icon-location"></i> @user.Location</td>*@
                            </tr>
                            <tr class="table-divider">
                                <th></th>
                                <td></td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
            <!--end of col-->
        </div>
        <!--end of row-->
    </div>
    <!--end of container-->
</section>